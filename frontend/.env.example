# Angular reads most local settings from source files and proxy.conf.json.
# This file exists to show that no frontend secrets are required for local development.
TESTCASEIQ_API_BASE_URL=http://localhost:8080
