spring:
  application:
    name: testcaseiq-api
  datasource:
    url: ${TESTCASEIQ_DATABASE_URL:jdbc:postgresql://localhost:5432/testcaseiq}
    username: ${TESTCASEIQ_DATABASE_USERNAME:testcaseiq}
    password: ${TESTCASEIQ_DATABASE_PASSWORD:testcaseiq_dev_password}
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8080
