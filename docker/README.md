# Docker

Local infrastructure for TestCaseIQ.

The root `docker-compose.yml` starts:

- PostgreSQL
- Redis

Only safe local development credentials are included. Do not place real secrets in this repository.
